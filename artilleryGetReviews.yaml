config:
  target: "http://localhost:4000"
  phases:
    - duration: 60
      arrivalRate: 500
scenarios:
  - flow:
    - get:
        url: "/reviews?product_id=19089"

# $(npm bin)/artillery run artilleryGetReviews.yaml