config:
  target: "http://localhost:4000"
  phases:
    - duration: 60
      arrivalRate: 1000
scenarios:
  - flow:
    - get:
        url: "/reviews/meta?product_id=19089"

# $(npm bin)/artillery run artilleryGetMeta.yaml
